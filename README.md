* Run `CognitoHelperApplication` with the vm args:

  * `-Daws.accessKeyId` < for access key >
  
  * `-Daws.secretAccessKe` < for secret access key >
  
  * `-Dpool.Name=tenant_v2` < for the pool we want to CRUD app client >